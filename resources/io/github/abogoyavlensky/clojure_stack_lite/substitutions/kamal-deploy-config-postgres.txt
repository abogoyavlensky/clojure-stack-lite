    - DATABASE_URL

# Database
accessories:
  db:
    image: postgres:18.1-alpine3.22
    host: <%= ENV['SERVER_IP'] %>
    env:
      secret:
        - POSTGRES_DB
        - POSTGRES_USER
        - POSTGRES_PASSWORD
    directories:
      - {{main/file}}_postgres_data:/var/lib/postgresql/data
